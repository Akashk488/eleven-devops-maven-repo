pipeline {
    agent {label 'jenkins_slave'}
    tools {
        maven 'maven-test'
    }
    stages {
        stage ('checkout-scm'){
            steps{
                checkout scm
            }
        }
        stage ('maven_build'){
            steps {
                sh 'mvn clean package'
            }
        }
        stage ('tomcat_deploy'){
            steps {
                sshagent(['tomcat-webserver']){
                    
                    sh "scp -o strictHostkeychecking=no target/maven-web-application.war ec2-user@13.233.196.219:/opt/tomcat9/webaspps"
                }
            }
        }
    }
}
