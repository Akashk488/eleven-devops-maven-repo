pipeline{
    agent {label 'jenkins_slave'}
    tools {
        maven 'maven_test'
    }

    stages {
        stage ('checkout_scm'){

            steps {

                checkout scm
            }
        }

        stage ('maven_build'){

            steps {

                sh 'mvn clean package'
            }
        }

        stage ('tomcat_deploys'){

            steps{
                sshagent (['tomcat_server']){

                    sh 'scp -o StrictHostKeyChecking=no target/maven-web-application.war ec2-user@13.234.136.249:/opt/tomcat9/webapps'
                }
            }
        }
    }
}
